# 服务访问目录说明

本目录统一通过网关访问后端，并承载后续由 OpenAPI 生成的 B 端客户端。允许访问健康检查、B 端身份和经正式契约授权的运营接口；禁止直接访问 `8081` 至 `8084`、数据库、Redis 或 C 端本人业务接口。

请求日志和错误提示不得包含登录凭据、支付密码、确认令牌、二维码原始令牌或完整账号。业务写操作必须在契约明确后携带服务端规定的幂等键并关闭自动重试。
