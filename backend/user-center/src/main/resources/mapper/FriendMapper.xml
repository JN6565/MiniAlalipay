<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minialalipay.user.infrastructure.persistence.mapper.FriendMapper">

    <insert id="insert">
        INSERT INTO user_db.friend (user_id, friend_user_id, alias, created_at)
        VALUES (#{userId}, #{friendUserId}, #{alias}, #{createdAt})
    </insert>

    <delete id="delete">
        DELETE FROM user_db.friend
        WHERE (user_id = #{userId} AND friend_user_id = #{friendUserId})
           OR (user_id = #{friendUserId} AND friend_user_id = #{userId})
    </delete>

    <select id="selectByUserAndFriend" resultType="com.minialalipay.user.infrastructure.persistence.po.FriendPO">
        SELECT user_id, friend_user_id, alias, created_at
        FROM user_db.friend
        WHERE user_id = #{userId} AND friend_user_id = #{friendUserId}
    </select>

    <select id="selectByUserId" resultType="com.minialalipay.user.infrastructure.persistence.po.FriendPO">
        SELECT user_id, friend_user_id, alias, created_at
        FROM user_db.friend
        WHERE user_id = #{userId}
        ORDER BY created_at DESC
        LIMIT #{limit}
    </select>

    <select id="countByUserAndFriend" resultType="int">
        SELECT COUNT(*) FROM user_db.friend
        WHERE (user_id = #{userId1} AND friend_user_id = #{userId2})
           OR (user_id = #{userId2} AND friend_user_id = #{userId1})
    </select>

</mapper>
