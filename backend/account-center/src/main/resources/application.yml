server:
  port: 8083
spring:
  application:
    name: account-center
  datasource:
    url: jdbc:mysql://39.107.95.141:3306/account_db?useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&characterEncoding=utf8mb4
    username: teamuser
    password: TeamPass@2026
    driver-class-name: com.mysql.cj.jdbc.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    default-schema: account_db
  mybatis:
    mapper-locations: classpath:mapper/*.xml
    configuration:
      map-underscore-to-camel-case: true
management:
  endpoints:
    web:
      exposure:
        include: health,info
