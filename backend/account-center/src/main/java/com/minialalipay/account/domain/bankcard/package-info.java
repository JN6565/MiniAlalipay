/**
 * 银行卡限界上下文领域层。
 *
 * <p>职责：银行卡绑定聚合（绑卡、设默认、解绑的状态流转与不变量）、
 * 银行卡号识别（BIN 字典与 Luhn 校验）、敏感信息掩码化规则与银行卡错误码。
 *
 * <p>边界：只依赖平台通用技术类型（错误码接口），禁止依赖 Spring MVC、
 * 持久化框架或其他限界上下文的领域包；完整卡号、证件号、手机号等明文
 * 只在绑卡入参中短暂出现，聚合内只允许保存掩码值。
 */
package com.minialalipay.account.domain.bankcard;
