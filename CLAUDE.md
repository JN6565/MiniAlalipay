# MiniAIalipay Claude 使用说明

修改任何内容前，必须先阅读并遵守根目录的 `AGENTS.md`。该文件是本仓库唯一的工程规范依据。

必须遵守以下约束：

1. 项目文档、接口说明、代码注释、错误提示和 Git 提交摘要使用中文；仅保留必要的技术英文标识符和协议字段。
2. 编码前必须阅读相关 PRD、系统分析、数据库设计和 OpenAPI 契约。
3. 若实现需要新增或改变系统分析未描述的能力，必须在同一次修改中同步更新系统分析和接口契约。
4. 前端只能调用 `http://localhost:8080`，禁止直接访问中心服务、MySQL 或 Redis。
5. 网关路由前缀在 OpenAPI、后端 Controller 和测试同时具备前均不可调用。
6. 领域模型和持久化类型必须留在所属限界上下文，`platform-common` 只放技术通用能力。
7. 只有 `account-center` 可以修改资金、信用或账本数据，金额使用整数分。
8. 遵守 Flyway 前向迁移与 `type(scope): 中文说明` 提交规范。
9. 禁止提交构建产物、依赖目录、本地状态、密钥、日志、IDE 文件或数据库备份。
